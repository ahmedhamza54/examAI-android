package tn.esprit.examaijetpack.ui.theme

import androidx.compose.ui.graphics.Color

// Define new colors to match the UI
val PrimaryRed = Color(0xFFE53935)      // Red for the button
val LightBackground = Color(0xFFFFFFFF) // White background
val InputFieldBorder = Color(0xFFCCCCCC) // Light grey for borders
val ErrorRed = Color(0xFFD32F2F)        // Error message color
val TextColor = Color(0xFF000000)       // Black text
val HintTextColor = Color(0xFF888888)   // Hint text color
val TermsTextColor = Color(0xFF007AFF)  // Blue for terms and conditions text

// Optional: You can define a darker version of the red for pressed states
val PrimaryRedDark = Color(0xFFC62828)
